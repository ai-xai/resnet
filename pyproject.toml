[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "resnet"
version = "1.0.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["resnet"]
package-dir = { "token_gen" = "src" }
